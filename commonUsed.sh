

# server current directory tree at http:$hostname:8000
python -m SimpleHttpServer

# run this command without saving it in history
<space> command

# place the argument on the shell
ALT + .

# print the current mount points in a pretty layout
mount | column -t

# get the external IP address of local machine
curl ifconfig.me 
